mutation updateUserPersonalization(
  $userPersonalization: UserPersonalizationInputType!
) {
  updateUserPersonalization(userPersonalization: $userPersonalization) {
    id
    user_id
    type
    value
  }
}
